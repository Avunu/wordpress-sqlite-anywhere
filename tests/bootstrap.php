<?php

declare(strict_types=1);

// PHPUnit bootstrap for the plugin's own suite (src/). The drop-in code is
// pure PHP over constants and environment variables; no WordPress is loaded.
require_once __DIR__ . '/../vendor/autoload.php';
